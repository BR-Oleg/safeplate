name: Test iOS App in Simulator

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master

env:
  FLUTTER_VERSION: '3.19.0'

jobs:
  test-ios-simulator:
    name: Test iOS Simulator
    runs-on: macos-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: 'stable'
          cache: true

      - name: Install CocoaPods
        run: |
          sudo gem install cocoapods
          pod --version

      # ADICIONADO: Limpeza profunda de cache do CocoaPods antes do 'flutter clean'
      - name: Clean CocoaPods Cache
        run: |
          pod cache clean --all || true

      - name: Clean Flutter (resolve Android embedding V2)
        run: flutter clean || true

      - name: Get Flutter dependencies
        run: |
          OUTPUT=$(flutter pub get 2>&1) || {
            EXIT_CODE=$?
            echo "$OUTPUT"
            # Verificar se é apenas o aviso do Android embedding V2
            if echo "$OUTPUT" | grep -q "Android embedding"; then
              echo "⚠️ Aviso do Android embedding detectado, mas configuração está correta (V2)"
              echo "✅ Continuando apesar do aviso..."
              exit 0
            else
              echo "❌ Erro diferente do Android embedding, falhando..."
              exit $EXIT_CODE
            fi
          }
          echo "$OUTPUT"

      - name: Generate iOS files (Flutter)
        run: |
          # Garantir que os arquivos iOS sejam gerados
          flutter precache --ios || true

      - name: Install iOS dependencies
        working-directory: ios
        run: |
          # Verificar se Podfile existe
          if [ ! -f "Podfile" ]; then
            echo "❌ Podfile não encontrado! Criando..."
            # O Podfile já deve estar no repositório, mas vamos verificar
            exit 1
          fi
          echo "✅ Podfile encontrado, instalando pods..."
          # Reinstalação de pods no diretório correto
          pod install --repo-update

      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      - name: List available simulators
        run: |
          echo "📱 Listando simuladores disponíveis..."
          xcrun simctl list devices available

      - name: Find and Boot iPhone Simulator
        run: |
          echo "🔍 Procurando simulador iPhone disponível..."
          
          # Listar todos os simuladores disponíveis
          echo "📱 Simuladores disponíveis:"
          xcrun simctl list devices available | grep -i "iphone" || echo "Nenhum iPhone encontrado"
          
          # Tentar encontrar iPhone Pro primeiro (qualquer versão), depois qualquer iPhone
          # Extrair o UDID (identificador único) do simulador
          # Formato da linha: "    iPhone 16 Pro (A95FF35F-EA71-4924-9A09-4C490F6F0FC0) (Shutdown)"
          DEVICE_LINE=$(xcrun simctl list devices available | grep -i "iPhone.*Pro" | head -1)
          
          # Se não encontrou Pro, procurar qualquer iPhone
          if [ -z "$DEVICE_LINE" ]; then
            DEVICE_LINE=$(xcrun simctl list devices available | grep -i "iPhone" | head -1)
          fi
          
          # Extrair UDID da linha (está entre parênteses, antes do status)
          DEVICE_UDID=$(echo "$DEVICE_LINE" | grep -oE '[A-F0-9]{8}-[A-F0-9]{4}-[A-F0-9]{4}-[A-F0-9]{4}-[A-F0-9]{12}' | head -1)
          
          if [ -z "$DEVICE_UDID" ]; then
            echo "❌ Nenhum simulador iPhone encontrado!"
            echo "Listando todos os dispositivos disponíveis:"
            xcrun simctl list devices available
            exit 1
          fi
          
          # Obter o nome completo do dispositivo da linha já capturada
          # Formato: "    iPhone 16 Pro (A95FF35F-...)"
          DEVICE_NAME=$(echo "$DEVICE_LINE" | sed -E 's/^[[:space:]]+([^(]+).*/\1/' | xargs)
          
          # Se não conseguiu extrair o nome, tentar pelo UDID
          if [ -z "$DEVICE_NAME" ] || [ "$DEVICE_NAME" = "$DEVICE_UDID" ]; then
            DEVICE_FULL_LINE=$(xcrun simctl list devices | grep "$DEVICE_UDID")
            DEVICE_NAME=$(echo "$DEVICE_FULL_LINE" | sed -E 's/^[[:space:]]+([^(]+).*/\1/' | xargs)
          fi
          
          # Se ainda não conseguiu, usar uma descrição genérica
          if [ -z "$DEVICE_NAME" ] || [ "$DEVICE_NAME" = "$DEVICE_UDID" ]; then
            DEVICE_NAME="iPhone Simulator"
          fi
          
          echo "✅ Simulador selecionado: $DEVICE_NAME (UDID: $DEVICE_UDID)"
          
          # Verificar se já está iniciado
          BOOTED=$(xcrun simctl list devices | grep "$DEVICE_UDID" | grep "Booted" || echo "")
          
          if [ -z "$BOOTED" ]; then
            echo "🚀 Iniciando simulador $DEVICE_NAME..."
            # Tentar iniciar pelo UDID (mais confiável)
            xcrun simctl boot "$DEVICE_UDID" || {
              echo "⚠️ Erro ao iniciar pelo UDID, aguardando e tentando novamente..."
              sleep 3
              xcrun simctl boot "$DEVICE_UDID" || {
                echo "❌ Falha ao iniciar simulador após múltiplas tentativas"
                echo "Status do dispositivo:"
                xcrun simctl list devices | grep "$DEVICE_UDID"
                exit 1
              }
            }
            echo "✅ Simulador iniciado com sucesso"
          else
            echo "✅ Simulador já está iniciado"
          fi
          
          # Aguardar simulador ficar pronto
          echo "⏳ Aguardando simulador ficar pronto (10 segundos)..."
          sleep 10
          
          # Verificar status final
          echo "📱 Status do simulador:"
          xcrun simctl list devices | grep "$DEVICE_UDID"
          
          # Salvar UDID e nome para próximos passos
          echo "DEVICE_UDID=$DEVICE_UDID" >> $GITHUB_ENV
          echo "DEVICE_NAME=$DEVICE_NAME" >> $GITHUB_ENV

      - name: Build app for simulator
        run: |
          echo "🔨 Compilando app para simulador iOS..."
          # MODIFICADO: Tentar o build e capturar o output e o código de saída
          OUTPUT=$(flutter build ios --simulator --release 2>&1)
          EXIT_CODE=$?
          
          echo "$OUTPUT"
          
          # Caminho esperado do artefato de simulador
          SIMULATOR_APP_PATH="build/ios/iphonesimulator/Runner.app"
          
          # Se o build falhou (EXIT_CODE != 0)
          if [ $EXIT_CODE -ne 0 ]; then
            # Verificar se a falha é *apenas* o aviso do Android embedding E se o arquivo foi criado (caso a falha seja um falso positivo)
            if echo "$OUTPUT" | grep -q "Android embedding" && [ -d "$SIMULATOR_APP_PATH" ]; then
              echo "⚠️ Aviso do Android embedding detectado, mas build do simulador criado com sucesso."
              echo "✅ Continuando apesar do aviso..."
              exit 0 # Sucesso, ignorando o erro
            else
              echo "❌ Erro fatal durante o build do simulador."
              echo "❌ Build iOS não foi criado ou erro não relacionado ao Android embedding, falhando..."
              exit $EXIT_CODE # Falha, mantendo o código de saída original
            fi
          fi
          
          # Se o build foi bem-sucedido (EXIT_CODE == 0)
          if [ -d "$SIMULATOR_APP_PATH" ]; then
            echo "✅ Build iOS para simulador criado com sucesso!"
          else
            echo "❌ Build iOS não foi criado (diretório Runner.app ausente), falhando..."
            exit 1
          fi
          
          echo "✅ App compilado para simulador"

      - name: Install app on simulator
        run: |
          echo "📦 Instalando app no simulador..."
          # Usar o UDID salvo ou 'booted' como fallback
          DEVICE_ID=${DEVICE_UDID:-booted}
          # CORRIGIDO: Usando 'iphonesimulator'
          xcrun simctl install "$DEVICE_ID" build/ios/iphonesimulator/Runner.app || {
            echo "⚠️ Tentando instalar no dispositivo booted..."
            xcrun simctl install booted build/ios/iphonesimulator/Runner.app || {
              echo "❌ Falha ao instalar app"
              exit 1
            }
          }
          echo "✅ App instalado no simulador"

      - name: Launch app on simulator
        run: |
          echo "🚀 Iniciando app no simulador..."
          # Usar o UDID salvo ou 'booted' como fallback
          DEVICE_ID=${DEVICE_UDID:-booted}
          xcrun simctl launch "$DEVICE_ID" com.safeplate.safeplate || {
            echo "⚠️ Tentando iniciar no dispositivo booted..."
            xcrun simctl launch booted com.safeplate.safeplate || {
              echo "⚠️ App pode já estar rodando ou houve erro ao iniciar"
            }
          }
          echo "✅ App iniciado (ou já estava rodando)"

      - name: Take screenshots
        run: |
          echo "📸 Tirando screenshots do simulador..."
          
          # Criar diretório para screenshots
          mkdir -p screenshots
          
          # Aguardar app carregar completamente
          echo "⏳ Aguardando app carregar (10 segundos)..."
          sleep 10
          
          # Tirar screenshot da tela inicial
          echo "📸 Screenshot 1: Tela inicial..."
          xcrun simctl io booted screenshot screenshots/01-inicial.png || echo "Screenshot 1 failed"
          sleep 2
          
          # Tirar mais screenshots
          echo "📸 Screenshot 2..."
          xcrun simctl io booted screenshot screenshots/02-tela2.png || echo "Screenshot 2 failed"
          sleep 2
          
          echo "📸 Screenshot 3..."
          xcrun simctl io booted screenshot screenshots/03-tela3.png || echo "Screenshot 3 failed"
          
          # Listar screenshots criados
          echo ""
          echo "✅ Screenshots capturados:"
          ls -lh screenshots/*.png 2>/dev/null || echo "Nenhum screenshot encontrado"

      - name: Record video (opcional)
        if: false  # Desabilitado por padrão (demora mais)
        run: |
          echo "🎥 Gravando vídeo do simulador..."
          xcrun simctl io booted recordVideo --codec=h264 --force screenshots/demo.mp4 &
          RECORD_PID=$!
          sleep 30  # Gravar por 30 segundos
          kill $RECORD_PID || true
          echo "✅ Vídeo gravado"

      - name: Get simulator logs
        run: |
          echo "📋 Capturando logs do simulador..."
          xcrun simctl spawn booted log stream --level=error --predicate 'processImagePath contains "Runner"' > screenshots/simulator-logs.txt 2>&1 &
          sleep 10
          pkill -f "log stream" || true
          echo "✅ Logs capturados"

      - name: Upload screenshots and logs
        uses: actions/upload-artifact@v4
        with:
          name: ios-simulator-screenshots-${{ github.run_number }}
          path: |
            screenshots/
          retention-days: 7

      - name: Display simulator info
        run: |
          echo "📱 Informações do Simulador:"
          if [ -n "$DEVICE_UDID" ]; then
            xcrun simctl list devices | grep "$DEVICE_UDID"
          else
            xcrun simctl list devices | grep "iPhone"
          fi
          echo ""
          echo "✅ App instalado e testado no simulador!"
          echo "📸 Screenshots disponíveis nos artifacts"