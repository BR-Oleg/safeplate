name: SafePlateNative
options:
  deploymentTarget:
    iOS: "14.0"

packages:
  MapboxMaps:
    url: https://github.com/mapbox/mapbox-maps-ios
    from: 11.8.0

targets:
  SafePlateNative:
    type: application
    platform: iOS
    sources:
      - path: Sources/SafePlateNative
    resources:
      - path: Resources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.safeplate.safeplate
        INFOPLIST_FILE: Resources/Info.plist
        CODE_SIGNING_ALLOWED: NO
    dependencies:
      - package: MapboxMaps
        product: MapboxMaps
  SafePlateNativeUITests:
    type: bundle.ui-testing
    platform: iOS
    sources:
      - path: Tests/SafePlateNativeUITests
    settings:
      base:
        TEST_TARGET_NAME: SafePlateNative
        GENERATE_INFOPLIST_FILE: YES
        CODE_SIGNING_ALLOWED: NO
    dependencies:
      - target: SafePlateNative

schemes:
  SafePlateNative:
    build:
      targets:
        SafePlateNative: all
    run:
      config: Debug
    test:
      config: Debug
      targets:
        - name: SafePlateNativeUITests
